<style type="text/css">
    .cuadro-inicio {
        max-width: 650px;
        height: 500px;
        padding: 19px 29px 29px;
        margin: 0 auto 10px;
        background: -moz-linear-gradient(-45deg, #75a7ce 0%, #456380 100%);
        background: -webkit-linear-gradient(-45deg, #75a7ce 0%, #456380 100%);
        background: -o-linear-gradient(-45deg, #75a7ce 0%, #456380 100%);
        background: linear-gradient(to bottom right, #75a7ce 0%, #456380 100%);
        -webkit-border-radius: 10px;
        -moz-border-radius: 0px;
        color: #FDF2F2;

    }
</style>

<div class="cuadro-inicio">

  <div style="text-align: center;">
    <h2>Sistema Comedor</h2>
    <br><br>

    <h4>Iniciar Sesión</h4>
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>

        <div class="control-group">
          <label class="control-label" for="inputEmail">Email</label>
          <div class="controls">
            <%= f.email_field :email, :autofocus => true %>
          </div>
        </div>

        <div class="control-group">
          <label class="control-label" for="inputPassword">Password</label>
          <div class="controls">
            <%= f.password_field :password %>
          </div>
        </div>

        <% if devise_mapping.rememberable? -%>
            <div><%= f.check_box :remember_me %>
              <%= f.label :remember_me, "Recordarme" %></div>
        <% end -%>

        <br>
        <div>
          <%= f.submit "Iniciar Sesión", :class => 'btn btn-primary'%>
        </div>

    <% end %>



    <strong>
      <div style="color: #bf5a16">
        <%- flash.each do |name, msg| -%>
            <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
        <%- end -%>
      </div>
    </strong>

  </div>




</div> <!-- /container -->